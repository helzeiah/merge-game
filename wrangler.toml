name               = "merge-game-worker"
main               = "worker.js"
compatibility_date = "2024-01-01"

[[kv_namespaces]]
binding = "KV"
id      = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"

# After deploying, set the signing secret (never commit this value):
#   npx wrangler secret put SECRET
